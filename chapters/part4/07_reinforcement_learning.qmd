# 7. 深層強化学習 (Reinforcement Learning)

### 問1

DQNにおいて、Q学習の一部である「TD誤差」は以下のように定義される。
次の空欄（あ）に入る適切な語句を選べ。
$$
TD誤差 = [あ] − Q(s, a)
$$
A. Q(s', a') の平均値  
B. 実際に得られた報酬  
C. ${γ * Q(s, a)}$  
D. ${R(s, a) + γ * maxQ(s', a')}$
